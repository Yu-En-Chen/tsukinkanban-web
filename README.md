<div align="center">

<img src="img/logo.png" alt="通勤看板ロゴ" width="96" height="96">

# 通勤看板

**首都圏の運行情報を、一枚のカードで。**

日本語 | [繁體中文](#繁體中文)

🌐 [www.tsukinkanban.com](https://www.tsukinkanban.com)

</div>

---

## 日本語

### 概要

**通勤看板**は、首都圏の鉄道運行情報と国内線フライト情報を、カード形式でひと目で確認できるモバイル向け Web アプリ（PWA）です。毎日の通勤・通学で「自分が使う路線だけ」をまとめて確認できることを目指して開発されました。

- 会員登録不要・広告なし・サードパーティ Cookie なし
- パーソナライズ設定はすべて端末内（ブラウザ）に保存され、サーバーには一切送信されません
- ホーム画面に追加すれば、ネイティブアプリのように利用できます（PWA 対応）

### 主な機能

| 機能 | 説明 |
|---|---|
| 🚃 運行情報カード | 登録した路線の運行状況をカードスタックで表示。カードをタップすると詳細を確認できます |
| ✈️ フライトカード | ANA・JAL など国内線の運航状況にも対応（羽田・成田） |
| 🔍 路線検索 | ヘッダーの検索からインクリメンタルに路線を検索・追加 |
| 🎨 パーソナライズ | カードの表示名やカラーを自由にカスタマイズ（端末内保存） |
| 🕐 同期時計 | 最終データ同期時刻を常時表示。同期が途切れると色で警告します |
| 🔔 通知・履歴 | 運行情報の変化を時系列で振り返ることができます |
| ↩️ 取り消し | パーソナライズ変更のワンタップ・アンドゥ |

### データソース

交通データは **公共交通オープンデータ協議会（[ODPT](https://www.odpt.org/)）** より提供されています。対応路線の一覧はアプリ内「データソース」ページをご覧ください。

> ⚠️ 本サービスは個人が開発・運営する**非公式**のサードパーティアプリであり、ODPT および各鉄道・交通事業者とは一切関係がありません。

### 技術構成

- **フレームワークなし** — 素の HTML / CSS / JavaScript（ES Modules）のみで構築。ビルド工程は不要です
- **PWA** — `manifest.json` によるホーム画面追加・スタンドアロン表示に対応
- **アイコン** — [Lucide Icons](https://lucide.dev/)（SVG インライン埋め込み）
- **状態保存** — `localStorage` / ブラウザ内 DB のみ。サーバー側にユーザーデータは保存しません
- **アクセシビリティ** — 主要な操作ボタンに `aria-label` を付与。JS でアイコンと機能が切り替わるボタンは、モード切替と同時にラベルも更新されます

### ディレクトリ構成

```
tsukinkanban-web/
├── index.html          # エントリーポイント（アプリシェル）
├── manifest.json       # PWA マニフェスト
├── css/                # スタイルシート（変数・アニメーション・各 UI 部品）
├── js/                 # アプリケーションロジック
│   ├── script.js       #   メインエンジン（カード描画・操作）
│   ├── header.js       #   ヘッダー・検索・カプセルボタンのモード切替
│   ├── clock.js        #   同期時計
│   ├── flights.js      #   フライトカード
│   ├── personalization.js  # カードのカスタマイズ
│   └── ...
├── data/               # 路線辞書・メニュー文言などの静的データ
└── img/                # ロゴ・画像
```

### ローカルで動かす

ビルド不要です。任意の静的サーバーでルートディレクトリを配信してください。

```bash
# 例 1: Python
python3 -m http.server 8000

# 例 2: VS Code の Live Server 拡張機能で index.html を開く
```

ブラウザで `http://localhost:8000` を開けば動作します（運行データの取得には外部 API への接続が必要です）。

### お問い合わせ

- 開発者: 台湾在住の個人開発者（[@yueneuy_c](https://x.com/yueneuy_c)）
- ご意見・不具合報告: [info@tsukinkanban.com](mailto:info@tsukinkanban.com)

---

## 繁體中文

### 簡介

**通勤看板**（つうきんかんばん）是一個以卡片形式呈現東京首都圈鐵路運行資訊與日本國內線航班資訊的行動網頁應用程式（PWA）。目標是讓每天通勤、通學的使用者「只看自己會用到的路線」，一眼掌握現在能不能順利出門。

- 免註冊、無廣告、不使用第三方 Cookie
- 所有個人化設定只存在你的裝置（瀏覽器）內，不會上傳到伺服器
- 支援 PWA，加入主畫面後可像原生 App 一樣使用

### 主要功能

| 功能 | 說明 |
|---|---|
| 🚃 運行資訊卡片 | 以卡片堆疊顯示你關注路線的運行狀況，點擊卡片查看詳細資訊 |
| ✈️ 航班卡片 | 支援 ANA、JAL 等日本國內線航班動態（羽田、成田） |
| 🔍 路線搜尋 | 從頂部搜尋列即時搜尋並加入路線 |
| 🎨 個人化 | 自訂卡片的顯示名稱與顏色（僅存於裝置內） |
| 🕐 同步時鐘 | 常駐顯示最後資料同步時刻，同步中斷時以顏色警示 |
| 🔔 通知・履歷 | 依時間軸回顧運行資訊的變化 |
| ↩️ 復原 | 個人化變更可一鍵復原 |

### 資料來源

交通資料由**公共交通開放資料協議會（[ODPT](https://www.odpt.org/)）**提供。支援路線清單請見 App 內的「資料來源」頁面。

> ⚠️ 本服務為個人開發、營運的**非官方**第三方應用程式，與 ODPT 及各鐵路、交通業者皆無任何關係。

### 技術架構

- **無框架** — 純 HTML / CSS / JavaScript（ES Modules），不需要任何建置流程
- **PWA** — 透過 `manifest.json` 支援加入主畫面與獨立視窗顯示
- **圖示** — [Lucide Icons](https://lucide.dev/)（以行內 SVG 嵌入）
- **狀態儲存** — 僅使用 `localStorage` 與瀏覽器內建資料庫，伺服器端不儲存任何使用者資料
- **無障礙** — 主要操作按鈕皆附有 `aria-label`；由 JS 切換圖示與功能的按鈕，會在切換模式的同時一併更新語音標籤

### 目錄結構

```
tsukinkanban-web/
├── index.html          # 進入點（App Shell）
├── manifest.json       # PWA 設定檔
├── css/                # 樣式表（變數、動畫、各 UI 元件）
├── js/                 # 應用程式邏輯
│   ├── script.js       #   主引擎（卡片繪製與操作）
│   ├── header.js       #   頂部列、搜尋、膠囊按鈕模式切換
│   ├── clock.js        #   同步時鐘
│   ├── flights.js      #   航班卡片
│   ├── personalization.js  # 卡片個人化
│   └── ...
├── data/               # 路線字典、選單文案等靜態資料
└── img/                # Logo 與圖片
```

### 本機執行

不需要建置，用任何靜態伺服器指向專案根目錄即可：

```bash
# 方法 1：Python
python3 -m http.server 8000

# 方法 2：用 VS Code 的 Live Server 擴充功能開啟 index.html
```

在瀏覽器打開 `http://localhost:8000` 即可運作（取得即時運行資料需要能連上外部 API）。

### 聯絡方式

- 開發者：住在台灣的個人開發者（[@yueneuy_c](https://x.com/yueneuy_c)）
- 意見回饋、問題回報：[info@tsukinkanban.com](mailto:info@tsukinkanban.com)
